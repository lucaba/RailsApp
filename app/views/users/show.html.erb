<% provide(:title, @user.name) %>
<div class="row">
    <aside class="span4">
        <section>
            <h1>
                <%= @user.name %>
            </h1>
        </section>
    </aside>
    <li><%= link_to "Configuration",   user_configurations_path(@user) %></li>
</div>